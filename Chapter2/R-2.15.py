# 2.3.3节的Vector类提供接受一个整数d的构造函数，并产生一个d维向量，它的所有坐标等于0.另一个创建矢量的便捷方式是给构造函数传递一个参数，
# 一些迭代类型可以代表一系列的数字，创建一个向量，它的维度等于序列的长度，坐标值等于序列值。例如，Vector([7,4,5])会产生一个三维向量，
# 坐标为<7,4,5>。修改构造函数，使它可以接受任何形式的参数。也就是说，如果一个整数被传递，它就产生了一个所有坐标值为零的向量。但是如果提供了一个序列，
# 它就产生另一个坐标值等于序列值的向量。
# 示例代码中已实现
