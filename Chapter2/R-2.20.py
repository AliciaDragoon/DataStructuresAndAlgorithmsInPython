# 拥有一颗非常深的继承树会有哪些潜在的效率劣势？也就是说，有一个很大的类的集合，A、B、C……，其中B继承自A、C继承自B、D继承自C……
# 深继承树最主要的直接效率劣势集中在：
# 虚函数调用开销增加（查找深度增加）；
# 对象构造/析构步骤增多；
# 对象内存占用增大（累积祖先数据成员）；
# RTTI操作变慢（类型检查/转换需要遍历长链）；
# 缓存局部性降低（数据分散在继承层级中）；
# 编译链接时间增长（依赖复杂）。
# 此外，深继承树带来的设计复杂性和脆弱性会间接导致开发效率降低、维护成本升高、系统稳定性下降，最终也可能影响到运行时性能和可扩展性。
